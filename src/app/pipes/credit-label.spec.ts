import { CreditLabelPipe } from './credit-label';

describe('CreditLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
